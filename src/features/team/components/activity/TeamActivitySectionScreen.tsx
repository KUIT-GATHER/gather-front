type TeamActivitySectionScreenProps = {
  title: string;
  emptyMessage: string;
};

export function TeamActivitySectionScreen(
  _props: TeamActivitySectionScreenProps,
) {
  void _props;

  return (
    <section className="px-5.5 py-5">
      <div>빈 화면</div>
    </section>
  );
}
